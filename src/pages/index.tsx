import React from 'react';
import MainMenu from '../components/ui/MainMenu';

const Home: React.FC = () => {
    return (
        <div>
            <MainMenu />
        </div>
    );
};

export default Home;